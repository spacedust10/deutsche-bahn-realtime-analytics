# --- Realtime ingestion -------------------------------------------------
requests==2.32.3               # HTTPS access to the GTFS-RT endpoint
gtfs-realtime-bindings==1.0.0  # Official GTFS-Realtime protobuf bindings
protobuf>=5.26                 # Protocol Buffers runtime (transitive, pinned)

# --- Storage ------------------------------------------------------------
psycopg2-binary==2.9.10        # PostgreSQL driver

# --- Analytics / ML -----------------------------------------------------
pandas==2.2.2
numpy>=1.26
scikit-learn==1.7.1
joblib>=1.3

# --- API / dashboard ----------------------------------------------------
fastapi==0.136.3
uvicorn[standard]==0.49.0
websockets>=12.0

# --- Tests --------------------------------------------------------------
pytest==9.1.1
httpx>=0.27                    # FastAPI TestClient transport
